[project]
name = "try-uiautomator2"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Henry.Huang", email = "hhglory@outlook.com" }
]
dependencies = [
    "uiautomator2>=3.1.0",
]
readme = "README.md"
requires-python = ">= 3.12"

#[build-system]
#requires = ["hatchling"]
#build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/try_uiautomator2"]



[tool.poetry]
name = "try-uiautomator2"
version = "0.1.0"
description = ""
authors = ["Henry.Huang <hhglory@outlook.com>"]
readme = "README.md"
packages = [{ include = "try_uiautomator2", from = "src" }]

[tool.poetry.dependencies]
python = "^3.12"
uiautomator2 = "^3.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
