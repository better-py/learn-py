[project]
name = "kivy-basic"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "hhglory", email = "hhglory@outlook.com" }
]
dependencies = [
    "kivymd>=1.2.0",
    "kivy[full]>=2.3.0",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/kivy_basic"]
